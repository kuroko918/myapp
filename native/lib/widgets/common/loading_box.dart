import 'package:flutter/material.dart';

showLoadingBox(double height, double width) {
  return Container(
    color: Colors.grey[400],
    child: SizedBox(
      height: height,
      width: width,
    ),
  );
}
